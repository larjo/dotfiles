1. Install chocolately
2. cinst git && cinst vim-tux

3. Start admin console
    cd $env:USERPROFILE
    git clone https://github.com/larjo/dotfiles.git
    dotfiles\install.bat (or if your not admin: nonadmin-install.bat)
